# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
vendor/telegram-built
!.env.example
dist
dist-ssr

# PWA manifest icons (generated by `scripts/generate-pwa-icons.mjs` before build)
public/pwa*
*.local

# Editor directories and files
.codemie/
.claude/
.cursor/
.github/
!.github/workflows/
!.github/workflows/*
.vscode/
!.vscode/extensions.json
.idea/
.specstory/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
AGENTS.md
CLAUDE.md


# >>> agentic-kit (managed) >>>
# Managed by agentic-kit. Re-run `agentic-kit/init.sh` to refresh, or
# `agentic-kit/teardown.sh` to remove the whole block. Edits inside this block
# are overwritten on init.
#
# --- Runtime / ephemeral (pipeline scratch; usually not committed) ---
.artefacts/memory/
.artefacts/features/
.artefacts/archive/
.artefacts/proposed-patches/
.artefacts/SESSION-STATE.md
.artefacts/MEMORY.md
.artefacts/PROJECT_PROFILE.md
#
# --- Per-machine kit bookkeeping (usually not committed) ---
.artefacts/.agentic-kit.cfg
.artefacts/.agentic-kit.files
#
# Optional — ignore all of .artefacts/ except PIPELINE.md + PROJECT.md (uncomment all 4 lines):
.artefacts/
# !.artefacts/
# !.artefacts/PIPELINE.md
# !.artefacts/PROJECT.md
#
# <<< agentic-kit (managed) <<<
