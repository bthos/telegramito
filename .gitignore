# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
vendor/telegram-built
!.env.example
dist
dist-ssr

# PWA manifest icons (generated by `scripts/generate-pwa-icons.mjs` before build)
public/pwa*
*.local

# Editor directories and files
.codemie/
.claude/
.cursor/
.cursor*
# Ignore local .github noise but keep workflows trackable (`.github/` alone blocks negation).
.github/*
!.github/workflows/
!.github/workflows/**
.vscode/
!.vscode/extensions.json
.idea/
.specstory/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
AGENTS.md
CLAUDE.md


# >>> agentic-kit (managed) >>>
# Managed by agentic-kit. Re-run `agentic-kit/init.sh` to refresh, or
# `agentic-kit/teardown.sh` to remove the whole block. Edits inside this block
# are overwritten on init.
#
.artefacts/
# !.artefacts/
# !.artefacts/PIPELINE.md
# !.artefacts/PROJECT.md
#
# <<< agentic-kit (managed) <<<
